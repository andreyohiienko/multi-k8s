apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      component: worker
  template: # Pod
    metadata:
      labels:
        component: worker
    spec:
      containers:
      - name: worker
        image: andreyohiienko/multi-worker
